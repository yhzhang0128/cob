use crate::error::OracleError;

pub fn remove_latency() -> Result<(), OracleError>  {
    println!("Removing latency");

    Ok(())
}

pub fn setup_latency() -> Result<(), OracleError>  {
    println!("Setting up latency");

    Ok(())
}
